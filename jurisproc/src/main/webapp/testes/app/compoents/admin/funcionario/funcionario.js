<template><nao_definido id="funionario.nome" value="{{ funionario.nome }}" />
		<nao_definido id="funionario.codigo" value="{{ funionario.codigo }}" />
		<nao_definido id="funionario.idade" value="{{ funionario.idade }}" />
		
    
</template>

<script>
export default {
    name: 'funcionario',
    props: ['titulo']
}
</script>