<template><nao_definido id="departamento.nome" value="{{ departamento.nome }}" />
		<nao_definido id="departamento.telefone" value="{{ departamento.telefone }}" />
		<nao_definido id="departamento.descricao" value="{{ departamento.descricao }}" />
		
    
</template>

<script>
export default {
    name: 'departamento',
    props: ['titulo']
}
</script>