<template><nao_definido id="usuario.nome" value="{{ usuario.nome }}" />
		<nao_definido id="usuario.sexo" value="{{ usuario.sexo }}" />
		
    
</template>

<script>
export default {
    name: 'novo_usuario',
    props: ['titulo']
}
</script>